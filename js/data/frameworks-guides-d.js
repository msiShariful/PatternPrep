/* Spring Boot guides — part D: Testing, Build & Deploy, Production Features. */
window.FRAMEWORK_TOPICS = window.FRAMEWORK_TOPICS || [];

window.FRAMEWORK_TOPICS.push({
  fw: "spring-boot",
  id: "testing",
  order: 9,
  title: "Testing Spring Boot",
  hue: "purple",
  tagline: "Unit tests, slice tests, MockMvc — fast where it counts",
  minutes: 12,
  summary: "The Boot testing toolkit sorted by weight: pure JUnit + Mockito tests that skip Spring entirely, slice tests like <code>@WebMvcTest</code> and <code>@DataJpaTest</code> that boot only one layer, and <code>@SpringBootTest</code> for the few tests that need everything.",
  blocks: [
    { type: "p", text: "The number one testing mistake in Boot projects is treating <code>@SpringBootTest</code> as the default — booting the entire application to check that a discount calculator multiplies correctly. Spring's testing story is a <strong>menu of context sizes</strong>: none at all (plain JUnit + Mockito), one slice (just the web layer, just the JPA layer), or the whole app. The skill isn't writing tests — it's ordering the smallest thing that proves your point, because a suite that takes thirty minutes stops being run, and a suite that stops being run stops being a suite." },
    { type: "callout", variant: "analogy", title: "Don't crash the whole car to test a brake pad", text: "Car makers don't do a full crash test to check a brake pad — they put the pad on a rig that presses it against a spinning disc. Crash tests exist, but they're rare, expensive, and test the <em>assembly</em>. Your layers deserve the same discipline: rigs (unit tests) for parts, harnesses (slice tests) for subsystems, and a handful of full crashes (<code>@SpringBootTest</code>) for the whole vehicle." },
    { type: "h3", text: "The pyramid, in Boot terms" },
    { type: "table", headers: ["Level", "What loads", "Speed", "Use for"], rows: [
      ["Plain JUnit + Mockito", "Nothing — no Spring at all", "milliseconds", "Business logic in services; the bulk of your tests"],
      ["<code>@WebMvcTest</code>", "One controller + MVC machinery; services mocked", "~1–2s", "Routing, status codes, JSON shapes, validation, advice"],
      ["<code>@DataJpaTest</code>", "Entities + repositories on in-memory DB", "~1–2s", "Query methods, mappings, relationship cascades"],
      ["<code>@SpringBootTest</code>", "The entire application context", "many seconds", "A few end-to-end flows wiring everything together"]
    ] },
    { type: "h3", text: "Unit tests — no Spring required" },
    { type: "p", text: "Here is where constructor injection pays its dividend: a service whose dependencies arrive through the constructor can be built in a test with <code>new</code> and hand-made fakes — no container, no annotations, no startup cost. <strong>Mockito</strong> manufactures the fakes: <code>@Mock</code> builds a stand-in repository, <code>when(...).thenReturn(...)</code> scripts its answers, <code>@InjectMocks</code> constructs the service around the mocks, and <code>verify</code> asserts the interactions you care about:" },
    { type: "code", lang: "java", text: "@ExtendWith(MockitoExtension.class)        // JUnit 5 + Mockito, zero Spring\nclass UserServiceTest {\n\n  @Mock UserRepository repo;               // fake repository, scripted below\n  @InjectMocks UserService service;        // real service, built around the mock\n\n  @Test\n  void findUser_mapsEntityToDto() {\n    when(repo.findById(7L))\n        .thenReturn(Optional.of(new User(\"Kim\", \"kim@mail.com\")));\n\n    UserDto dto = service.findUser(7L);\n\n    assertEquals(\"Kim\", dto.name());\n    verify(repo).findById(7L);             // interaction we insist on\n  }\n\n  @Test\n  void findUser_missing_throws() {\n    when(repo.findById(9L)).thenReturn(Optional.empty());\n    assertThrows(UserNotFoundException.class, () -> service.findUser(9L));\n  }\n}" },
    { type: "callout", variant: "rule", title: "If it can be a unit test, it is a unit test", text: "Reach for a Spring context only when the thing under test IS Spring behavior — routing, serialization, queries, wiring. A business rule that needs a container to be tested is usually a class with too many jobs; the test friction is design feedback." },
    { type: "h3", text: "@WebMvcTest + MockMvc — the web layer alone" },
    { type: "p", text: "Controllers are worth testing for what <em>they</em> own: does the route bind, does bad input give 400, does the JSON look right, does the advice map exceptions to statuses? <code>@WebMvcTest(UserController.class)</code> boots only the MVC slice for that controller — no services, no database — and you mock the service boundary with <code>@MockBean</code> (Mockito mock, registered as the bean). <strong>MockMvc</strong> then performs requests <em>without a real server</em> — it drives the DispatcherServlet directly:" },
    { type: "code", lang: "java", text: "@WebMvcTest(UserController.class)          // just this controller's slice\nclass UserControllerTest {\n\n  @Autowired MockMvc mvc;\n  @MockBean UserService service;           // the boundary, mocked\n\n  @Test\n  void get_returnsJson() throws Exception {\n    when(service.findUser(7L)).thenReturn(new UserDto(7L, \"Kim\", \"kim@mail.com\"));\n\n    mvc.perform(get(\"/users/7\"))\n       .andExpect(status().isOk())\n       .andExpect(jsonPath(\"$.name\").value(\"Kim\"))\n       .andExpect(jsonPath(\"$.email\").value(\"kim@mail.com\"));\n  }\n\n  @Test\n  void get_missing_is404() throws Exception {\n    when(service.findUser(9L)).thenThrow(new UserNotFoundException(9L));\n\n    mvc.perform(get(\"/users/9\"))\n       .andExpect(status().isNotFound())              // advice did its job\n       .andExpect(jsonPath(\"$.code\").value(\"USER_NOT_FOUND\"));\n  }\n}" },
    { type: "h3", text: "@DataJpaTest — the persistence layer alone" },
    { type: "p", text: "The JPA slice boots entities, repositories, and an in-memory H2 — nothing else — and wraps <strong>each test in a transaction that rolls back</strong>, so tests can't contaminate each other. This is where derived query methods and relationship mappings earn their keep: <code>findByEmailAndActiveTrue</code> is generated code, but the <em>name</em> encodes your intent, and only a test proves the intent matches:" },
    { type: "code", lang: "java", text: "@DataJpaTest                               // entities + repos + H2, rollback per test\nclass UserRepositoryTest {\n\n  @Autowired UserRepository repo;\n\n  @Test\n  void findByActive_skipsInactive() {\n    repo.save(new User(\"Kim\", \"kim@mail.com\"));          // active by default\n    User off = new User(\"Lee\", \"lee@mail.com\");\n    off.deactivate();\n    repo.save(off);\n\n    List<User> found = repo.findByActiveTrue();\n\n    assertEquals(1, found.size());\n    assertEquals(\"Kim\", found.get(0).getName());\n  }\n}" },
    { type: "callout", variant: "pitfall", title: "H2 is not PostgreSQL", text: "Slice tests on H2 validate your JPA mappings, not your database. Native queries, JSON columns, sequence quirks, and locking behavior can pass on H2 and fail on PostgreSQL. When a query leans on database-specific behavior, run that test against the real engine — Testcontainers spins up a disposable PostgreSQL in Docker per test run, and modern Boot (3.1+) integrates it with a single <code>@ServiceConnection</code> annotation." },
    { type: "h3", text: "@SpringBootTest — the whole vehicle" },
    { type: "p", text: "Sometimes the point IS the assembly: security filters + controller + service + database, one request flowing through everything. <code>@SpringBootTest</code> boots the full context; add <code>webEnvironment = RANDOM_PORT</code> to start the real embedded server on a free port and hit it over actual HTTP with <code>TestRestTemplate</code> or <code>WebTestClient</code> (the default, <code>MOCK</code>, skips the real server and uses MockMvc-style calls instead). The context is heavyweight but <strong>cached across tests that share a configuration</strong> — which is why you keep configurations uniform, and why sprinkling <code>@MockBean</code> differently in every test class quietly multiplies your boot count." },
    { type: "code", lang: "java", text: "@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)\nclass UserFlowIT {\n\n  @Autowired TestRestTemplate http;        // real HTTP against the random port\n\n  @Test\n  void createThenFetch_roundTrips() {\n    ResponseEntity<UserDto> created =\n        http.postForEntity(\"/users\", new NewUser(\"Kim\", \"kim@mail.com\"), UserDto.class);\n    assertEquals(HttpStatus.CREATED, created.getStatusCode());\n\n    UserDto fetched = http.getForObject(\"/users/\" + created.getBody().id(), UserDto.class);\n    assertEquals(\"Kim\", fetched.name());   // through security, MVC, service, JPA, H2\n  }\n}" },
    { type: "callout", variant: "pro", title: "Guard the context cache like a build artifact", text: "Spring caches test contexts keyed on their exact configuration — profiles, properties, and the set of @MockBeans all count. Five test classes with five slightly different configurations = five full boots. Fast suites standardize: one shared integration-test base class with fixed configuration, mocks declared once, and every IT extending it. A 40-second suite versus a 15-minute one is usually just this discipline." },
    { type: "check", items: [
      { q: "Why does constructor injection make services testable without Spring?", a: "Dependencies arrive as constructor parameters, so a test builds the service with <code>new</code> and Mockito fakes — no container needed to assemble the object." },
      { q: "What does @WebMvcTest load, and what must you provide?", a: "Only the web slice for the named controller — MVC machinery, converters, advice. Anything it injects (services) must be supplied, typically as @MockBean mocks." },
      { q: "Why do @DataJpaTest tests not pollute each other?", a: "Each test runs in a transaction that rolls back at the end — inserts vanish before the next test starts." },
      { q: "When is @SpringBootTest the right tool?", a: "When the assembly is the subject: a flow crossing security, web, service, and persistence together. A few of these; everything else goes to cheaper levels." },
      { q: "What silently makes a Boot test suite slow, beyond test count?", a: "Context cache misses: every distinct configuration (differing properties, profiles, @MockBean sets) forces another full application boot. Uniform configs = few boots." }
    ] },
    { type: "ladder", steps: [
      { t: "Learn", d: "Recite the four levels with what loads at each, and the rule that picks between them: smallest thing that proves the point." },
      { t: "Drill", d: "Write the trio for one feature: a Mockito service test, a @WebMvcTest asserting a 404 JSON body, and a @DataJpaTest for a derived query." },
      { t: "Interview-ready", d: "Explain @WebMvcTest vs @SpringBootTest, what @MockBean does to the context, and why the pyramid matters — then learn to ship what you tested.", href: "#/frameworks/spring-boot/build-run-deploy", link: "Build, Run, Deploy" },
      { t: "Master", d: "Adopt the shared-base-class pattern in a suite, measure boots before and after, then swap one H2 slice test to Testcontainers PostgreSQL and see what it catches.", href: "#/frameworks/spring-boot", link: "Roadmap map" }
    ] }
  ]
});

window.FRAMEWORK_TOPICS.push({
  fw: "spring-boot",
  id: "build-run-deploy",
  order: 10,
  title: "Build, Run, Deploy",
  hue: "orange",
  tagline: "Maven, Gradle, the fat jar, and config that travels",
  minutes: 12,
  summary: "The path from source to server: what Maven and Gradle actually do, why the Boot parent means no version numbers, how the executable fat jar with its embedded Tomcat (or Jetty) works, and configuring deployments with profiles, environment variables, and external files.",
  blocks: [
    { type: "p", text: "Code that only runs in your IDE isn't software yet. The last mile — dependencies resolved, tests run, everything zipped into something a server can start — is owned by a build tool, and Boot's best trick lives here: the <strong>executable fat jar</strong>. One file containing your classes, every library, and a web server, launched with nothing but <code>java -jar</code>. Deployment used to mean installing and configuring an application server; now it means copying one file. This guide covers the tools that build that file, the server hiding inside it, and the config discipline that lets one build run everywhere." },
    { type: "callout", variant: "analogy", title: "The camper van, not the hotel", text: "Old-style Java deployment was hotel living: your app (a WAR file) checked into a big external Tomcat that ops installed, patched, and shared among apps — and every hotel was configured slightly differently. The fat jar is a camper van: the engine and the bedroom travel <strong>with</strong> the app. Any machine with Java (or any container) is a parking spot. \"Works in my van\" is the whole story — there is no hotel to differ." },
    { type: "h3", text: "Maven — the default build tool" },
    { type: "p", text: "Maven reads one file, <code>pom.xml</code>, and does four jobs: <strong>resolve dependencies</strong> (download libraries + their own dependencies from repositories), <strong>compile</strong>, <strong>run tests</strong>, and <strong>package</strong>. Boot projects inherit from the <code>spring-boot-starter-parent</code>, and that inheritance is why your dependencies have no version numbers — the parent's <code>dependencyManagement</code> section pins tested, compatible versions for hundreds of libraries. One property (<code>&lt;version&gt;</code> of the parent) upgrades the entire stack coherently:" },
    { type: "code", lang: "xml", text: "<project>\n  <parent>\n    <groupId>org.springframework.boot</groupId>\n    <artifactId>spring-boot-starter-parent</artifactId>\n    <version>3.3.4</version>                 <!-- pins EVERYTHING below -->\n  </parent>\n\n  <groupId>com.example</groupId>\n  <artifactId>shop</artifactId>\n  <version>0.1.0</version>\n\n  <dependencies>\n    <dependency>\n      <groupId>org.springframework.boot</groupId>\n      <artifactId>spring-boot-starter-web</artifactId>   <!-- no version! -->\n    </dependency>\n    <dependency>\n      <groupId>org.springframework.boot</groupId>\n      <artifactId>spring-boot-starter-data-jpa</artifactId>\n    </dependency>\n  </dependencies>\n\n  <build>\n    <plugins>\n      <plugin>  <!-- the Boot plugin: repackages into the executable fat jar -->\n        <groupId>org.springframework.boot</groupId>\n        <artifactId>spring-boot-maven-plugin</artifactId>\n      </plugin>\n    </plugins>\n  </build>\n</project>" },
    { type: "h3", text: "Gradle — the same jobs, different style" },
    { type: "p", text: "Gradle does everything Maven does with a script (<code>build.gradle</code>, in Groovy or Kotlin) instead of XML, and earns its following with speed: incremental builds, a build cache, and a daemon that keeps the JVM warm make big projects rebuild in a fraction of the time. The honest comparison: <strong>Maven</strong> is rigid, verbose, and utterly predictable — every Maven project looks the same, which is a feature at 3am. <strong>Gradle</strong> is faster and more flexible, and flexibility means one project's build script can become its own little application. Both are first-class citizens for Boot (start.spring.io offers both); teams pick on taste and existing muscle memory. Learn to <em>read</em> both, master one." },
    { type: "h3", text: "From source to fat jar" },
    { type: "steps", title: "What `mvn package` actually does", frames: [
      { d: "Resolve: read the pom, download every dependency (and their dependencies) into the local cache.", cells: { cells: [{ v: "resolve", hl: 1 }, { v: "compile", dim: true }, { v: "test", dim: true }, { v: "package", dim: true }, { v: "repackage", dim: true }] } },
      { d: "Compile src/main/java against those libraries.", cells: { cells: [{ v: "resolve", hl: 2 }, { v: "compile", hl: 1 }, { v: "test", dim: true }, { v: "package", dim: true }, { v: "repackage", dim: true }] } },
      { d: "Run the test suite — a red test stops the line; you cannot package broken code by accident.", cells: { cells: [{ v: "resolve", hl: 2 }, { v: "compile", hl: 2 }, { v: "test", hl: 1 }, { v: "package", dim: true }, { v: "repackage", dim: true }] } },
      { d: "Package the compiled classes into a plain, thin jar — your code only, not yet runnable on its own.", cells: { cells: [{ v: "resolve", hl: 2 }, { v: "compile", hl: 2 }, { v: "test", hl: 2 }, { v: "package", hl: 1 }, { v: "repackage", dim: true }] } },
      { d: "The Boot plugin repackages: your classes + all dependency jars + embedded Tomcat + a launcher, in one executable file. This is the artifact you ship.", cells: { cells: [{ v: "resolve", hl: 2 }, { v: "compile", hl: 2 }, { v: "test", hl: 2 }, { v: "package", hl: 2 }, { v: "repackage", hl: 1 }], pointers: [{ i: 4, t: "fat jar" }] } }
    ] },
    { type: "p", text: "During development you don't package at all: <code>mvn spring-boot:run</code> (or <code>./gradlew bootRun</code>) compiles and starts the app in place, and both accept profile switches — <code>mvn spring-boot:run -Dspring-boot.run.profiles=dev</code>. The plugin owns both tricks: the dev-time runner and the repackaging." },
    { type: "h3", text: "JAR vs WAR — and why WAR lost" },
    { type: "p", text: "A <strong>WAR</strong> is the hotel-era format: a web archive you deploy <em>into</em> an external application server that ops maintains separately. It still exists for organizations contractually wedded to a shared Tomcat/WebSphere, and Boot can produce one. But the default and the industry answer is the executable <strong>JAR</strong>: self-contained, versioned as a unit, identical on a laptop and in a container — which is also why it fits Docker so naturally (a Dockerfile for a Boot app is essentially \"copy the jar, run java -jar\"). Unless something external forces WAR, ship the jar." },
    { type: "h3", text: "Embedded servers — Tomcat by default, Jetty by choice" },
    { type: "p", text: "<code>spring-boot-starter-web</code> quietly includes <strong>Tomcat</strong> — the reason <code>java -jar</code> serves HTTP. Tomcat is the battle-hardened default and the right answer absent a reason; <strong>Jetty</strong> is the common alternative — lighter-footed, popular in embedded and low-overhead setups. Swapping is a dependency exercise, not a code change, which is itself the lesson: the server is a <em>library</em> now, not infrastructure:" },
    { type: "code", lang: "xml", text: "<dependency>\n  <groupId>org.springframework.boot</groupId>\n  <artifactId>spring-boot-starter-web</artifactId>\n  <exclusions>\n    <exclusion>   <!-- evict Tomcat… -->\n      <groupId>org.springframework.boot</groupId>\n      <artifactId>spring-boot-starter-tomcat</artifactId>\n    </exclusion>\n  </exclusions>\n</dependency>\n<dependency>      <!-- …seat Jetty -->\n  <groupId>org.springframework.boot</groupId>\n  <artifactId>spring-boot-starter-jetty</artifactId>\n</dependency>" },
    { type: "h3", text: "Deployment basics — one jar, many environments" },
    { type: "p", text: "The golden rule of deployment: <strong>build once, configure everywhere</strong>. The same tested jar goes to staging and production; only the configuration around it changes. Boot gives you three dials, in ascending override priority: <strong>profile files</strong> baked into the jar (<code>application-prod.yml</code>, activated by <code>--spring.profiles.active=prod</code>), an <strong>external config folder</strong> (a <code>config/application.yml</code> sitting next to the jar wins over the ones inside — ops can patch settings with a text editor, no rebuild), and <strong>environment variables</strong>, the container-era favorite. Boot's <em>relaxed binding</em> maps <code>SPRING_DATASOURCE_URL</code> to <code>spring.datasource.url</code> automatically — which is exactly how Kubernetes secrets and Docker <code>-e</code> flags reach your app:" },
    { type: "code", lang: "text", text: "# the whole deploy, three ways to configure it:\n\njava -jar shop-0.1.0.jar --spring.profiles.active=prod     # 1. profile switch\n\n./config/application.yml                                    # 2. external file next to jar\n#   (beats the application.yml inside the jar)\n\nexport SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/shop # 3. environment variables\nexport SPRING_DATASOURCE_PASSWORD=$(vault read ...)         #    (beat the files)\njava -jar shop-0.1.0.jar" },
    { type: "callout", variant: "pitfall", title: "Secrets are config — but config files are not vaults", text: "The externalized-config system makes it <em>easy</em> to put database passwords in <code>application-prod.yml</code> — and then they're in git, in every clone, forever (history doesn't forget). Real secrets ride environment variables or a secret manager (Vault, AWS Secrets Manager, Kubernetes Secrets) and enter the property space at launch. The yml files hold placeholders like <code>${DB_PASSWORD}</code>; the environment fills them." },
    { type: "callout", variant: "rule", title: "The artifact is immutable; the environment is not", text: "If staging and production run <em>different builds</em>, your staging tests proved nothing. Promote the identical jar (or the container image wrapping it) through environments, and express every difference as configuration. When a deploy misbehaves, this rule is what makes \"what changed?\" answerable." },
    { type: "callout", variant: "pro", title: "Layered jars make Docker builds fast", text: "A fat jar is one blob, so any code change invalidates Docker's cache for the whole ~80MB layer. Boot can split the jar into <em>layers</em> — dependencies (change rarely) separate from your classes (change constantly) — via its layertools mode, and buildpacks (<code>mvn spring-boot:build-image</code>) do this automatically. Result: rebuilding an image after a one-line change re-uploads kilobytes, not the world." },
    { type: "check", items: [
      { q: "Why do Boot dependencies in the pom need no version numbers?", a: "The spring-boot-starter-parent's dependencyManagement pins tested versions for hundreds of libraries; bumping the parent version upgrades the whole aligned stack." },
      { q: "What exactly is inside a fat jar?", a: "Your compiled classes, every dependency jar, the embedded server (Tomcat by default), and a small Boot launcher that wires the classpath and calls your main — everything java -jar needs." },
      { q: "When would you still build a WAR?", a: "Only when an external application server is imposed — organizational policy or legacy infrastructure. Otherwise the self-contained executable jar is the default." },
      { q: "How does ops change a setting without rebuilding the jar?", a: "External config: a config/application.yml next to the jar overrides the packaged one, and environment variables (SPRING_DATASOURCE_URL via relaxed binding) override files." },
      { q: "Maven or Gradle — what's the honest trade?", a: "Same fundamental jobs. Maven: rigid, verbose, uniform across projects. Gradle: faster (incremental + cache + daemon), flexible scripts — flexibility that can grow its own complexity. Team taste decides." }
    ] },
    { type: "ladder", steps: [
      { t: "Learn", d: "Narrate the five build stages from memory, and the three config dials with their priority order: files in jar < external folder < environment variables." },
      { t: "Drill", d: "mvn package your app, run the jar with --spring.profiles.active=prod, then override one property three ways and confirm which wins." },
      { t: "Interview-ready", d: "Explain the fat jar vs WAR shift and why embedded servers changed deployment — then meet the endpoints that watch the app you just shipped.", href: "#/frameworks/spring-boot/advanced", link: "Production Features" },
      { t: "Master", d: "Swap Tomcat for Jetty and prove nothing else changed; then build a layered Docker image and measure the rebuild after a one-line edit.", href: "#/system-design", link: "System Design" }
    ] }
  ]
});

window.FRAMEWORK_TOPICS.push({
  fw: "spring-boot",
  id: "advanced",
  order: 11,
  title: "Production Features",
  hue: "indigo",
  tagline: "Actuator, caching, schedules, async, and calling other services",
  minutes: 13,
  summary: "The features that separate a demo from a service: Actuator's health, info, and metrics endpoints, <code>@Cacheable</code> memoization and its eviction discipline, scheduled and async work, file uploads done safely, and WebClient vs RestTemplate for outbound calls.",
  blocks: [
    { type: "p", text: "Everything so far makes an application <em>work</em>. This guide is about what makes one <em>operable</em>: the difference between \"it runs\" and \"we can run it\" — knowing it's healthy without ssh-ing in, answering repeated questions from cache instead of the database, doing nightly work without a cron server, not blocking request threads on slow neighbors, and calling other services like a citizen. Boot ships all of it; the craft is in the defaults you change and the traps you route around." },
    { type: "callout", variant: "analogy", title: "The cockpit, not the engine", text: "An airliner doesn't make pilots open the cowling mid-flight to check the engines — gauges in the cockpit report fuel, temperature, and pressure continuously. Actuator bolts that cockpit onto your app: standardized gauges (health, metrics, info) that ops dashboards, load balancers, and Kubernetes read without ever opening the code. Production readiness starts with being observable from outside." },
    { type: "h3", text: "Actuator — ops endpoints for free" },
    { type: "p", text: "Add <code>spring-boot-starter-actuator</code> and your app grows a management API under <code>/actuator</code>. The big three: <strong><code>/actuator/health</code></strong> aggregates checks Boot auto-detects — database reachable? disk space? — into one UP/DOWN verdict, with dedicated <em>liveness</em> (\"restart me?\") and <em>readiness</em> (\"send me traffic?\") probes that Kubernetes consumes directly. <strong><code>/actuator/info</code></strong> serves build and git metadata — which commit is actually running in prod (add the build-info goal and git-commit-id plugin to populate it). <strong><code>/actuator/metrics</code></strong> exposes Micrometer's numbers — request latencies, JVM memory, GC pauses, connection pool usage — and Micrometer is a facade like SLF4J: add <code>micrometer-registry-prometheus</code> and <code>/actuator/prometheus</code> appears, ready for Prometheus to scrape and Grafana to chart." },
    { type: "code", lang: "yaml", text: "management:\n  endpoints:\n    web:\n      exposure:\n        include: health,info,metrics,prometheus   # explicit allow-list\n  endpoint:\n    health:\n      probes:\n        enabled: true          # /health/liveness + /health/readiness for k8s\n# Only health is web-exposed by default — the rest is opt-in, for good reason." },
    { type: "callout", variant: "pitfall", title: "Actuator is an attack surface", text: "Endpoints like <code>/actuator/env</code> and <code>/actuator/heapdump</code> will happily serve your environment variables and a full memory snapshot — credentials included. Never <code>include: \"*\"</code> on an internet-facing app. Expose the minimal set, lock <code>/actuator/**</code> behind auth or a separate management port, and let the load balancer see only <code>/health</code>." },
    { type: "h3", text: "Custom actuator endpoints" },
    { type: "p", text: "When ops keeps asking the same app-specific question — \"how deep is the import backlog?\" — give the answer a gauge instead of a shell session. <code>@Endpoint</code> classes plug into the same machinery as the built-ins, appearing under <code>/actuator/&lt;id&gt;</code> with the same security and exposure rules:" },
    { type: "code", lang: "java", text: "@Component\n@Endpoint(id = \"queues\")                 // → GET /actuator/queues\nclass QueueEndpoint {\n  private final ImportQueue queue;\n  QueueEndpoint(ImportQueue queue) { this.queue = queue; }\n\n  @ReadOperation\n  Map<String, Object> stats() {\n    return Map.of(\"depth\", queue.depth(), \"oldestMinutes\", queue.oldestAgeMin());\n  }\n}" },
    { type: "h3", text: "Caching — @Cacheable and the eviction discipline" },
    { type: "p", text: "Some answers are expensive to compute and rarely change — a product catalog, exchange rates, a rendered report. <code>@EnableCaching</code> plus <code>@Cacheable</code> memoizes a method: first call runs the body and stores the result under a key built from the arguments; repeat calls with the same arguments return the stored answer without executing the method at all. The store behind it is pluggable — a simple in-memory map by default, Caffeine for smarter local caching, <strong>Redis in production</strong> when multiple instances must share one cache and survive restarts:" },
    { type: "code", lang: "java", text: "@Service\nclass ProductService {\n\n  @Cacheable(\"products\")                        // key = the id argument\n  ProductDto find(long id) {\n    return expensiveLookup(id);                 // runs on miss only\n  }\n\n  @CacheEvict(value = \"products\", key = \"#id\")  // stale entry dies on update\n  void update(long id, ProductUpdate change) {\n    repo.update(id, change);\n  }\n\n  @CacheEvict(value = \"products\", allEntries = true)  // nuclear option\n  void reimportCatalog() { ... }\n}\n// @CachePut exists too: always run the method, refresh the cache with the result." },
    { type: "callout", variant: "rule", title: "Every @Cacheable needs an eviction story", text: "The classic caching bug isn't a missing cache — it's a cache nobody invalidates, serving last week's price with full confidence. Before adding <code>@Cacheable</code>, answer in writing: <em>when does this entry die?</em> An <code>@CacheEvict</code> on every write path, a TTL in the cache config, or both. No answer = no cache; a slow correct answer beats a fast wrong one." },
    { type: "h3", text: "Scheduling — cron without a cron server" },
    { type: "p", text: "Nightly cleanups, hourly syncs, minute-by-minute polls: <code>@EnableScheduling</code> once, then <code>@Scheduled</code> per job. Three triggers: <code>fixedRate</code> (start every N ms, regardless of how long the last run took), <code>fixedDelay</code> (start N ms <em>after the previous run finished</em> — the safe choice for jobs that might run long), and <code>cron</code> expressions for calendar schedules. One caution that finds every team eventually: by default all scheduled methods share <strong>one</strong> thread, so a hung job silently starves every other schedule — configure a pool if you run several:" },
    { type: "code", lang: "java", text: "@Component\nclass Housekeeping {\n\n  @Scheduled(cron = \"0 0 3 * * *\")            // 03:00 every night\n  void purgeExpiredTokens() { tokens.deleteExpired(); }\n\n  @Scheduled(fixedDelay = 60_000)             // 1 min AFTER the last run ends\n  void pollPartnerFeed() { feed.fetchNew(); }\n}" },
    { type: "h3", text: "Async — don't make the user wait for the email" },
    { type: "p", text: "A signup request that sends a welcome email inline holds the HTTP thread (and the user) hostage to your mail provider's latency. <code>@EnableAsync</code> plus <code>@Async</code> moves a method call onto a background thread pool: the caller returns immediately, the work happens elsewhere. Return <code>void</code> for fire-and-forget or <code>CompletableFuture&lt;T&gt;</code> when someone eventually wants the result. The same proxy rule as <code>@Transactional</code> applies — <strong>self-invocation silently runs synchronously</strong>, because a call from inside the class never crosses the proxy that does the thread hop. And configure the pool: the default executor's unbounded queue means overload shows up as memory growth instead of pushback:" },
    { type: "code", lang: "java", text: "@Service\nclass SignupService {\n  private final MailService mail;\n  SignupService(MailService mail) { this.mail = mail; }\n\n  void register(NewUser user) {\n    saveUser(user);\n    mail.sendWelcome(user.email());   // returns instantly — runs on the async pool\n  }\n}\n\n@Service\nclass MailService {\n  @Async                              // different bean — the proxy is crossed\n  void sendWelcome(String to) { smtp.send(to, \"Welcome!\"); }\n}" },
    { type: "h3", text: "File uploads — streams, not memory" },
    { type: "p", text: "Uploads arrive as <code>MultipartFile</code> parameters — but the type signature is the easy part. The rules that keep you out of incidents: cap sizes in config (Boot defaults to a stingy 1MB per file — raise it deliberately, not to \"unlimited\"), <strong>stream</strong> the content onward (<code>transferTo</code> a temp file or pipe <code>getInputStream()</code> to S3 — never <code>getBytes()</code> on user-controlled sizes, which is an OutOfMemory waiting for a large file), and treat the client's filename and content-type as attacker input — generate your own storage keys:" },
    { type: "code", lang: "java", text: "@PostMapping(value = \"/documents\", consumes = MediaType.MULTIPART_FORM_DATA_VALUE)\nResponseEntity<DocDto> upload(@RequestParam(\"file\") MultipartFile file) throws IOException {\n  if (file.isEmpty()) return ResponseEntity.badRequest().build();\n  String key = UUID.randomUUID() + \".pdf\";       // OUR name, not theirs\n  storage.put(key, file.getInputStream(), file.getSize());   // streamed, not buffered\n  return ResponseEntity.status(201).body(new DocDto(key));\n}" },
    { type: "code", lang: "yaml", text: "spring:\n  servlet:\n    multipart:\n      max-file-size: 20MB\n      max-request-size: 25MB" },
    { type: "h3", text: "Calling other services — RestTemplate vs WebClient" },
    { type: "p", text: "Your app is also a client: payment gateways, partner APIs, sibling microservices. <strong>RestTemplate</strong> is the classic — synchronous, blocking, one method call per HTTP exchange; ubiquitous in existing code and officially in maintenance mode (it works, it just stopped evolving). <strong>WebClient</strong> is the modern answer: a fluent builder, non-blocking under the hood, equally usable in blocking style (<code>.block()</code>) or reactive pipelines, plus first-class timeouts, retries, and streaming. New code takes WebClient (or, in Boot 3.2+, its declarative-interface cousins); the comparison in code:" },
    { type: "code", lang: "java", text: "// RestTemplate — classic, blocking\nRestTemplate rt = new RestTemplate();\nRateDto rate = rt.getForObject(\"https://fx.example/rates/USD\", RateDto.class);\n\n// WebClient — modern, fluent, timeout built into the pipeline\nWebClient web = WebClient.create(\"https://fx.example\");\nRateDto rate2 = web.get()\n    .uri(\"/rates/{cur}\", \"USD\")\n    .retrieve()\n    .bodyToMono(RateDto.class)\n    .timeout(Duration.ofSeconds(2))     // slow dependency ≠ my outage\n    .block();                            // blocking style is fine on MVC" },
    { type: "callout", variant: "pro", title: "Every outbound call needs a deadline", text: "The quiet killer in service-to-service calls is the missing timeout: a dependency hangs, your threads queue up waiting on it, your pool exhausts, and <em>you</em> are now down — cascading failure in its natural habitat. Set connect and response timeouts on every client, add bounded retries only for idempotent requests, and treat \"what happens when this dependency is slow?\" as a design question, not an incident retro." },
    { type: "check", items: [
      { q: "Liveness vs readiness — why does Kubernetes want both?", a: "Liveness = \"is the process broken? restart it.\" Readiness = \"can it take traffic right now?\" A booting or cache-warming app is alive but not ready; conflating them causes restart loops or traffic to unready pods." },
      { q: "Why is exposing all actuator endpoints dangerous?", a: "Endpoints like /env and /heapdump leak environment variables and full memory contents — credentials included. Expose an allow-list, secure /actuator/**, keep the rest internal." },
      { q: "What's the classic @Cacheable bug and its cure?", a: "Stale data: caching without invalidation. Every cache needs an eviction story — @CacheEvict on write paths and/or a TTL — decided before the annotation lands." },
      { q: "Why did an @Async method on the same class run synchronously?", a: "Self-invocation: the async hop lives in a proxy around the bean, and internal calls never cross it. Same trap as @Transactional — put the method on another bean." },
      { q: "fixedRate vs fixedDelay?", a: "fixedRate fires on a metronome regardless of run duration (runs can pile up); fixedDelay waits N ms after the previous run finishes — safer for jobs with variable duration." }
    ] },
    { type: "ladder", steps: [
      { t: "Learn", d: "Name the big three actuator endpoints and their consumers (load balancer, ops, Prometheus), and recite the eviction rule: no cache without a death plan." },
      { t: "Drill", d: "Add actuator with the allow-list config, watch /health flip when you stop your database, then cache one expensive method and prove the evict works." },
      { t: "Interview-ready", d: "Explain cache invalidation as a design question, the self-invocation proxy trap (async AND transactional), and why every outbound call needs a timeout." },
      { t: "Master", d: "Wire micrometer-registry-prometheus, chart one custom business metric in Grafana, and set an alert on it — the full observability loop, end to end.", href: "#/system-design", link: "System Design" }
    ] }
  ]
});
