/* Spring Boot guides — part B: Web MVC, REST APIs, Data JPA. */
window.FRAMEWORK_TOPICS = window.FRAMEWORK_TOPICS || [];

window.FRAMEWORK_TOPICS.push({
  fw: "spring-boot",
  id: "web-mvc",
  order: 3,
  title: "Web: MVC & REST Controllers",
  hue: "blue",
  tagline: "From HTTP request to method call — and the layers behind it",
  minutes: 13,
  summary: "How a URL becomes a call to your Java method, why apps split into controller, service, repository, and DTO layers, and where filters, interceptors, and global error handling fit in the pipeline.",
  blocks: [
    { type: "p", text: "Type <code>localhost:8080/users/7</code> into a browser and, somewhere in your app, a Java method runs with <code>7</code> as an argument. Nothing about that is magic — there is a precise pipeline between the network socket and your code, and knowing its stations is what separates \"I copy controller snippets\" from \"I know where to put things.\" This guide walks the whole road: the dispatcher that routes every request, the three layers your logic should live in, the DTOs that guard your API, and the two interception points — filters and interceptors — that see traffic before your controller does." },
    { type: "callout", variant: "analogy", title: "The hotel front desk", text: "A hotel guest never wanders the corridors looking for the housekeeper. Every request — towels, taxis, complaints — goes to the <strong>front desk</strong>, which knows exactly which department handles what. Spring MVC's <code>DispatcherServlet</code> is that front desk: every HTTP request lands on it first, it looks up which controller method claims that URL and verb, and it dispatches. One entrance, one routing brain." },
    { type: "h3", text: "Spring MVC in Boot — the request pipeline" },
    { type: "p", text: "The embedded Tomcat accepts the connection and parses raw bytes into an HTTP request. From there, <strong>every single request</strong> is handed to one servlet: the <code>DispatcherServlet</code>, Spring's <em>front controller</em> — auto-configured by Boot, mapped to <code>/</code>. It consults its handler mappings (built at startup by scanning your <code>@GetMapping</code>/<code>@PostMapping</code> annotations), picks the matching controller method, converts path variables and the JSON body into Java arguments, invokes the method, and finally lets Jackson turn the return value back into JSON. Your controller never touches sockets, headers, or serialization — the pipeline does." },
    { type: "graph", title: "One request, end to end", nodes: [
      { id: "c", v: "client", x: 4, y: 50 },
      { id: "f", v: "filter", x: 20, y: 50 },
      { id: "d", v: "dispatch", x: 36, y: 50, hl: 1 },
      { id: "i", v: "intercept", x: 52, y: 50 },
      { id: "ctl", v: "controller", x: 68, y: 50 },
      { id: "s", v: "service", x: 84, y: 30 },
      { id: "r", v: "repo", x: 84, y: 70 },
      { id: "db", v: "DB", x: 96, y: 70 }
    ], edges: [
      { a: "c", b: "f", dir: true }, { a: "f", b: "d", dir: true }, { a: "d", b: "i", dir: true },
      { a: "i", b: "ctl", dir: true }, { a: "ctl", b: "s", dir: true }, { a: "s", b: "r", dir: true }, { a: "r", b: "db", dir: true }
    ], caption: "Filters run before Spring even sees the request; the DispatcherServlet routes; interceptors wrap the controller; the response walks the same road backwards." },
    { type: "h3", text: "The layers — controller, service, repository" },
    { type: "p", text: "You <em>could</em> write SQL inside a controller method. Every real codebase forbids it, because mixing concerns makes each of them untestable and unchangeable. The standard split gives every class one job: the <strong>controller</strong> speaks HTTP (parse input, pick status codes — no business rules), the <strong>service</strong> holds the business logic (rules, calculations, transactions — no HTTP, no SQL), and the <strong>repository</strong> talks to the database (queries — no rules). Swap the database, and only repositories change. Add a rule, and only a service changes. Expose a new endpoint shape, and only a controller changes." },
    { type: "code", lang: "java", text: "@RestController\n@RequestMapping(\"/users\")               // CONTROLLER — speaks HTTP, nothing else\nclass UserController {\n  private final UserService service;\n  UserController(UserService service) { this.service = service; }\n\n  @GetMapping(\"/{id}\")\n  UserDto get(@PathVariable long id) {\n    return service.findUser(id);        // delegate immediately\n  }\n}\n\n@Service                                 // SERVICE — business rules live here\nclass UserService {\n  private final UserRepository repo;\n  UserService(UserRepository repo) { this.repo = repo; }\n\n  UserDto findUser(long id) {\n    User u = repo.findById(id)\n        .orElseThrow(() -> new UserNotFoundException(id));\n    return UserDto.from(u);             // entity never escapes this layer\n  }\n}\n\ninterface UserRepository extends JpaRepository<User, Long> { }  // REPOSITORY" },
    { type: "h3", text: "The DTO layer — don't ship your database to the browser" },
    { type: "p", text: "A <strong>DTO</strong> (data transfer object) is a plain class shaped for the API, separate from the <code>@Entity</code> shaped for the database. Returning entities directly from controllers feels like less work and goes wrong three ways: you leak columns you never meant to expose (<code>passwordHash</code>, internal flags), Jackson trips over lazily-loaded relations and either explodes or triggers a storm of queries mid-serialization, and every innocent database refactor silently becomes a breaking API change. A DTO with a <code>from(entity)</code> factory method is ten lines that decouple your public contract from your storage schema:" },
    { type: "code", lang: "java", text: "record UserDto(long id, String name, String email) {   // what the API promises\n  static UserDto from(User u) {                         // explicit, auditable mapping\n    return new UserDto(u.getId(), u.getName(), u.getEmail());\n    // no passwordHash, no roles, no lazy collections — by construction\n  }\n}" },
    { type: "callout", variant: "rule", title: "Thin controllers, rich services, dumb repositories", text: "A controller method should read like a sentence: take input, call one service method, return the result. If you see an <code>if</code> chain of business rules in a controller — or a repository method making decisions — code is living on the wrong floor. The test: could you reuse the service from a scheduled job with no HTTP around it? If not, HTTP concerns leaked in." },
    { type: "h3", text: "Exception handling — one place for all errors" },
    { type: "p", text: "Without help, an exception in a controller becomes an ugly 500 page with a stack trace. The wrong fix is try/catch in every endpoint. The right fix is a global translator: a <code>@ControllerAdvice</code> class whose <code>@ExceptionHandler</code> methods catch exceptions from <strong>every</strong> controller and turn each kind into the right status code and a clean JSON body. Your services throw meaningful exceptions; one advice class decides what the world sees:" },
    { type: "code", lang: "java", text: "@RestControllerAdvice                     // applies to ALL controllers\nclass ApiErrorHandler {\n\n  @ExceptionHandler(UserNotFoundException.class)\n  ResponseEntity<ApiError> notFound(UserNotFoundException e) {\n    return ResponseEntity.status(404)\n        .body(new ApiError(\"USER_NOT_FOUND\", e.getMessage()));\n  }\n\n  @ExceptionHandler(MethodArgumentNotValidException.class)   // @Valid failures\n  ResponseEntity<ApiError> invalid(MethodArgumentNotValidException e) {\n    return ResponseEntity.badRequest()\n        .body(new ApiError(\"VALIDATION_FAILED\", firstMessage(e)));\n  }\n}\n\nrecord ApiError(String code, String message) { }" },
    { type: "callout", variant: "pitfall", title: "Never let a stack trace reach a client", text: "A raw 500 with a stack trace hands attackers your class names, library versions, and SQL fragments — and hands users gibberish. Every production API needs a catch-all <code>@ExceptionHandler(Exception.class)</code> that logs the full trace <strong>server-side</strong> and returns a bland <code>{\"code\":\"INTERNAL_ERROR\"}</code>. Log loudly, respond quietly." },
    { type: "h3", text: "Filters vs interceptors — the two checkpoints" },
    { type: "p", text: "Two different mechanisms let you touch every request without editing every controller, and choosing between them is a favorite interview question. A <strong>filter</strong> belongs to the servlet container — it runs <em>before Spring MVC even sees the request</em>, sees raw request/response objects, and can short-circuit entirely (this is where Spring Security lives). An <strong>interceptor</strong> belongs to Spring MVC — it runs after routing, so it knows <em>which controller method</em> is about to run, and can hook before the handler, after it, and after the response is written." },
    { type: "table", headers: ["", "Filter", "Interceptor"], rows: [
      ["Belongs to", "Servlet container (pre-Spring)", "Spring MVC"],
      ["Runs", "Before/after the <strong>entire</strong> chain", "Around the <strong>controller</strong> call"],
      ["Knows the target method?", "✗ — just a URL", "✓ — full handler info"],
      ["Can short-circuit", "✓ — never reaches Spring", "✓ — before the controller"],
      ["Typical jobs", "Security, CORS, compression, request logging", "Auth checks needing route info, timing, locale, audit"]
    ] },
    { type: "callout", variant: "pro", title: "Order is a real design surface", text: "Filters form a chain with explicit ordering (<code>@Order</code> / registration order), and security is deliberately early — by the time your code runs, the user is already authenticated. When writing your own, extend <code>OncePerRequestFilter</code>: async dispatches and forwards can push a naive filter to run twice for one logical request, and idempotence bugs from that are miserable to find." },
    { type: "check", items: [
      { q: "What does the DispatcherServlet actually do?", a: "It's the front controller: every request reaches it first; it looks up the controller method mapped to that URL + verb, binds arguments (path variables, JSON body), invokes the method, and hands the return value to Jackson for the response." },
      { q: "Why can't controllers just return entities?", a: "Entities leak private columns, drag lazy-loaded relations into serialization, and weld your public API to your database schema — any refactor becomes a breaking change. DTOs make the contract explicit." },
      { q: "Where does business logic belong, and what's the test for it?", a: "In services. The test: the service must be reusable with no HTTP around it (from a scheduled job, a queue consumer). If it needs a request object, HTTP leaked in." },
      { q: "Filter or interceptor: you need to know which controller method will handle the request?", a: "Interceptor — it runs after routing and receives handler info. Filters run before Spring MVC and only see the raw URL." },
      { q: "How do you turn a service exception into a 404 without try/catch in controllers?", a: "A <code>@RestControllerAdvice</code> class with an <code>@ExceptionHandler</code> for that exception type — one global translator from exceptions to status codes and JSON error bodies." }
    ] },
    { type: "ladder", steps: [
      { t: "Learn", d: "Draw the pipeline from memory: client, filter, DispatcherServlet, interceptor, controller, service, repository — and say what each station is allowed to know." },
      { t: "Drill", d: "Build the three-layer user endpoint above, then add a @RestControllerAdvice that maps a custom NotFoundException to a clean 404 JSON body." },
      { t: "Interview-ready", d: "Nail the two classics: filters vs interceptors (container vs Spring, what each knows), and why DTOs exist. Then learn to design the endpoints themselves.", href: "#/frameworks/spring-boot/rest-apis", link: "RESTful API Design" },
      { t: "Master", d: "Write a OncePerRequestFilter that assigns every request a correlation id, log it in the advice's catch-all handler, and trace one request across all layers.", href: "#/frameworks/spring-boot", link: "Roadmap map" }
    ] }
  ]
});

window.FRAMEWORK_TOPICS.push({
  fw: "spring-boot",
  id: "rest-apis",
  order: 4,
  title: "RESTful API Design",
  hue: "teal",
  tagline: "Verbs, nouns, status codes, and docs that write themselves",
  minutes: 11,
  summary: "The conventions that make an API predictable: five HTTP methods used honestly, resource URLs built from plural nouns, correct status codes, JSON exchange with Jackson, and OpenAPI docs generated straight from your code.",
  blocks: [
    { type: "p", text: "REST is not a technology — it's a set of conventions for using HTTP the way it was designed, so that a developer who has never seen your API can guess how it works. The convention has two halves: <strong>nouns in the URL</strong> name the things (resources), and <strong>HTTP methods</strong> are the verbs that act on them. Get both right and your API needs almost no explaining; get them wrong and every endpoint needs a paragraph of documentation and a warning." },
    { type: "callout", variant: "analogy", title: "A well-organized warehouse", text: "A good warehouse has labeled aisles (<code>/products</code>), numbered shelves (<code>/products/42</code>), and four standard operations any worker understands: look at an item, add one, replace one, remove one. Nobody invents a new operation called <code>/doProductStuff</code>. REST is that discipline applied to URLs: standard shelves, standard operations, no surprises." },
    { type: "h3", text: "Resources — nouns, plural, hierarchical" },
    { type: "p", text: "A resource URL names a <strong>thing</strong>, never an action — the action comes from the HTTP method. Use plural nouns (<code>/orders</code>, not <code>/order</code> or <code>/getOrders</code>), nest only where one thing truly belongs to another (<code>/users/7/orders</code> — orders of user 7), and push everything optional into query parameters: <code>/orders?status=SHIPPED&amp;page=2&amp;sort=createdAt,desc</code> for filtering, paging, and sorting." },
    { type: "table", headers: ["Smell", "Better", "Why"], rows: [
      ["<code>GET /getUserById?id=7</code>", "<code>GET /users/7</code>", "The verb is the method; the URL names the thing"],
      ["<code>POST /users/7/delete</code>", "<code>DELETE /users/7</code>", "HTTP already has a delete verb — use it"],
      ["<code>GET /users/7/orders/all</code>", "<code>GET /users/7/orders</code>", "The collection IS all of them; filters go in query params"],
      ["<code>POST /createNewOrder</code>", "<code>POST /orders</code>", "POST to a collection means \"add one\""],
      ["<code>GET /api?action=refund&amp;order=9</code>", "<code>POST /orders/9/refund</code>", "One honest exception: true actions that aren't CRUD get a sub-resource"]
    ] },
    { type: "h3", text: "The five verbs" },
    { type: "table", headers: ["Method", "Meaning", "Idempotent?", "Typical success"], rows: [
      ["<code>GET</code>", "Read — never changes anything", "✓ (and safe)", "<code>200 OK</code>"],
      ["<code>POST</code>", "Create a new member in a collection", "✗ — repeat = duplicate", "<code>201 Created</code> + <code>Location</code> header"],
      ["<code>PUT</code>", "Replace a resource wholesale", "✓ — same body, same result", "<code>200</code> or <code>204 No Content</code>"],
      ["<code>PATCH</code>", "Partial update — only the sent fields", "✗ (usually)", "<code>200 OK</code>"],
      ["<code>DELETE</code>", "Remove the resource", "✓ — deleting twice = still gone", "<code>204 No Content</code>"]
    ] },
    { type: "p", text: "<strong>Idempotent</strong> is the word interviewers reach for: an operation is idempotent when doing it twice leaves the world exactly as doing it once. <code>DELETE /users/7</code> twice? User is gone either way — idempotent. <code>POST /orders</code> twice? Two orders — not idempotent, which is precisely why browsers warn you before resubmitting a form, and why payment APIs add idempotency keys on top of POST." },
    { type: "code", lang: "java", text: "@RestController\n@RequestMapping(\"/api/v1/books\")\nclass BookController {\n  private final BookService service;\n  BookController(BookService service) { this.service = service; }\n\n  @GetMapping                                    // GET /api/v1/books?page=0&size=20\n  Page<BookDto> list(Pageable page) { return service.list(page); }\n\n  @GetMapping(\"/{id}\")                           // GET /api/v1/books/5 → 200 or 404\n  BookDto get(@PathVariable long id) { return service.get(id); }\n\n  @PostMapping                                   // POST → 201 + Location: /books/9\n  ResponseEntity<BookDto> create(@Valid @RequestBody NewBook body) {\n    BookDto saved = service.create(body);\n    return ResponseEntity\n        .created(URI.create(\"/api/v1/books/\" + saved.id()))\n        .body(saved);\n  }\n\n  @PutMapping(\"/{id}\")                           // full replace\n  BookDto replace(@PathVariable long id, @Valid @RequestBody NewBook body) {\n    return service.replace(id, body);\n  }\n\n  @DeleteMapping(\"/{id}\")                        // DELETE → 204, body-less\n  ResponseEntity<Void> delete(@PathVariable long id) {\n    service.delete(id);\n    return ResponseEntity.noContent().build();\n  }\n}" },
    { type: "callout", variant: "rule", title: "Status codes are the contract", text: "Clients program against your status codes, so use them honestly: <code>200</code> read/updated, <code>201</code> created, <code>204</code> done-nothing-to-say, <code>400</code> malformed request, <code>401</code> who are you, <code>403</code> you specifically may not, <code>404</code> no such thing, <code>409</code> conflicts with current state, <code>422</code> understood but invalid, <code>500</code> our bug. The unforgivable sin is <code>200</code> with <code>{\"success\": false}</code> in the body — it breaks every client's error handling, every monitor, and every retry policy at once." },
    { type: "h3", text: "JSON in, JSON out — and the XML option" },
    { type: "p", text: "You almost never serialize by hand. <code>@RestController</code> marks every return value as the response body, and <strong>Jackson</strong> — auto-configured by Boot — converts Java objects to JSON on the way out and JSON to <code>@RequestBody</code> parameters on the way in, driven by the <code>Content-Type</code> and <code>Accept</code> headers. That header-driven choice is <strong>content negotiation</strong>: the same endpoint can serve XML to a client that asks for it — add the <code>jackson-dataformat-xml</code> dependency and clients sending <code>Accept: application/xml</code> get XML, no controller changes. In practice, modern APIs are JSON-first and XML survives mainly in enterprise integrations." },
    { type: "code", lang: "http", text: "POST /api/v1/books HTTP/1.1\nContent-Type: application/json\n\n{ \"title\": \"Clean Code\", \"pages\": 464 }\n\nHTTP/1.1 201 Created\nLocation: /api/v1/books/9\nContent-Type: application/json\n\n{ \"id\": 9, \"title\": \"Clean Code\", \"pages\": 464 }" },
    { type: "h3", text: "Versioning — surviving your own success" },
    { type: "p", text: "The moment real clients depend on your API, breaking changes need an escape hatch. The most common answer is the path prefix you saw above — <code>/api/v1/books</code> — because it's visible, cacheable, and trivially routable; header-based versioning (<code>Accept: application/vnd.app.v2+json</code>) keeps URLs clean at the cost of discoverability. Whichever you pick, the deeper rule is: <strong>additive changes are free</strong> (new optional fields never break a well-built client), so design payloads you can extend, and burn a version number only when you must remove or reshape." },
    { type: "h3", text: "Documentation — Swagger/OpenAPI and Postman" },
    { type: "p", text: "<strong>OpenAPI</strong> is a standard, machine-readable description of an API; <strong>Swagger UI</strong> renders it as interactive docs. In Boot, one dependency — <code>springdoc-openapi-starter-webmvc-ui</code> — scans your controllers, parameters, DTOs, and validation annotations at runtime and serves the result at <code>/swagger-ui.html</code>: every endpoint, every schema, with a Try-it-out button. Sprinkle <code>@Operation(summary = \"…\")</code> where names alone don't explain. <strong>Postman</strong> is the complement for manual work: save requests into collections, switch environments (local/staging), and share the collection with the team — it can even import your OpenAPI spec, so the generated docs and the manual test suite stay one artifact." },
    { type: "callout", variant: "pro", title: "Generated docs can't lie", text: "A wiki page describing an API is out of date the week after it's written. Docs generated from the code — routes from mappings, schemas from DTOs, constraints from <code>@Valid</code> rules — are re-derived on every build, so they are correct by construction. This is also the quiet argument for strong typing and bean validation: every constraint you encode is documentation you never have to write." },
    { type: "callout", variant: "pitfall", title: "The verb-in-URL relapse", text: "Under deadline pressure every team rediscovers <code>POST /users/7/sendWelcomeEmail</code>. Before minting an action endpoint, ask: is this really a state change on a resource? \"Send welcome email\" is often <code>POST /users/7/welcome-emails</code> — creating a thing you can then <code>GET</code> for status. Model actions as resources with lifecycles and the API stays uniform; scatter RPC verbs and it decays into a function list." },
    { type: "check", items: [
      { q: "Why is PUT idempotent but POST is not?", a: "PUT replaces a known resource with a given body — replaying it produces the same final state. POST adds a new member to a collection — replaying it adds another one." },
      { q: "What belongs in the URL path vs query parameters?", a: "Path: the identity of the resource (<code>/users/7/orders</code>). Query params: everything optional — filters, pagination, sorting (<code>?status=SHIPPED&amp;page=2</code>)." },
      { q: "A create endpoint succeeded. What exactly should it return?", a: "<code>201 Created</code>, a <code>Location</code> header pointing at the new resource, and typically the created representation in the body." },
      { q: "How does one endpoint serve both JSON and XML?", a: "Content negotiation: Jackson picks the converter matching the request's <code>Accept</code> header. With <code>jackson-dataformat-xml</code> on the classpath, <code>Accept: application/xml</code> gets XML from the same controller." },
      { q: "Why do generated OpenAPI docs beat a hand-written wiki?", a: "They're derived from the actual mappings, DTOs, and validation rules on every build — they can't drift from reality, and Swagger UI makes them executable." }
    ] },
    { type: "ladder", steps: [
      { t: "Learn", d: "Recite the five verbs with their idempotency and success codes, and rewrite three bad URLs from the smell table without peeking." },
      { t: "Drill", d: "Build the books controller above, then add springdoc-openapi and explore your own API in Swagger UI — try the failure cases too (404, validation 400)." },
      { t: "Interview-ready", d: "Be ready for \"design a REST API for X\" — nouns, verbs, status codes, pagination, versioning — and for \"what does idempotent mean?\" with the DELETE-twice example. The data behind these endpoints comes next.", href: "#/frameworks/spring-boot/data-jpa", link: "Data Access with JPA" },
      { t: "Master", d: "Design one non-CRUD action (refund, publish, retry) as a resource with a lifecycle instead of an RPC verb, and defend the choice.", href: "#/database", link: "Databases & SQL" }
    ] }
  ]
});

window.FRAMEWORK_TOPICS.push({
  fw: "spring-boot",
  id: "data-jpa",
  order: 5,
  title: "Data Access with JPA",
  hue: "green",
  tagline: "Repositories, entities, transactions — SQL without writing SQL",
  minutes: 15,
  summary: "How Spring Data JPA turns an interface into a full data layer: entities and repositories, query methods derived from names, pagination, projections, the Hibernate machinery underneath, and <code>@Transactional</code> done right.",
  blocks: [
    { type: "p", text: "Most backends are, honestly, a database with opinions — so the data layer is where Boot saves you the most code. The stack has three levels and it pays to know which one you're talking to: <strong>JPA</strong> is a standard API for mapping Java objects to tables, <strong>Hibernate</strong> is the engine that implements it (generating the actual SQL), and <strong>Spring Data JPA</strong> sits on top and eliminates the boilerplate — you declare an interface, and a full CRUD implementation appears at runtime. You write Java; SQL happens." },
    { type: "callout", variant: "analogy", title: "The bilingual clerk", text: "A repository is a clerk who speaks fluent Java on one side of the counter and fluent SQL on the other. You say \"find me customers named Kim who joined this year\" in Java (<code>findByNameAndJoinedAfter(...)</code>); the clerk translates to SQL, negotiates with the database, and hands back Java objects. You never learn the database's dialect — but a good developer still peeks at what the clerk actually said, because clumsy translations get expensive." },
    { type: "h3", text: "Entities — Java classes with a table behind them" },
    { type: "code", lang: "java", text: "@Entity\n@Table(name = \"users\")                       // table name (defaults to class name)\nclass User {\n\n  @Id\n  @GeneratedValue(strategy = GenerationType.IDENTITY)   // DB assigns the id\n  private Long id;\n\n  @Column(nullable = false, length = 120)\n  private String name;\n\n  @Column(unique = true, nullable = false)\n  private String email;\n\n  private boolean active = true;             // plain fields map automatically\n\n  protected User() { }                       // JPA needs a no-arg constructor\n  User(String name, String email) { this.name = name; this.email = email; }\n  // getters …\n}" },
    { type: "h3", text: "JpaRepository — CRUD for the price of an interface" },
    { type: "p", text: "Extend <code>JpaRepository&lt;User, Long&gt;</code> (entity type + id type) and Spring Data generates the implementation at startup. Without writing a single method you get the full CRUD kit: <code>save</code>, <code>findById</code>, <code>findAll</code>, <code>count</code>, <code>existsById</code>, <code>deleteById</code>, batch variants, and more — plus everything below." },
    { type: "code", lang: "java", text: "interface UserRepository extends JpaRepository<User, Long> { }\n\n// usage — all of this is generated:\nUser saved = repo.save(new User(\"Kim\", \"kim@mail.com\"));   // INSERT (or UPDATE)\nOptional<User> u = repo.findById(5L);                      // SELECT by primary key\nList<User> all  = repo.findAll();                          // SELECT *\nlong n          = repo.count();                            // SELECT COUNT(*)\nrepo.deleteById(5L);                                       // DELETE" },
    { type: "h3", text: "Derived query methods — the method name IS the query" },
    { type: "p", text: "Here is Spring Data's party trick: declare a method whose name follows a grammar — <code>findBy</code> / <code>countBy</code> / <code>existsBy</code> + field names + operators like <code>And</code>, <code>Or</code>, <code>After</code>, <code>Containing</code>, <code>OrderBy</code> — and the query is <strong>parsed from the name at startup</strong> and implemented for you. A typo in a field name fails the app at boot, not at 3am in production:" },
    { type: "code", lang: "java", text: "interface UserRepository extends JpaRepository<User, Long> {\n\n  Optional<User> findByEmail(String email);                 // WHERE email = ?\n  List<User> findByActiveTrueOrderByNameAsc();              // WHERE active = true ORDER BY name\n  List<User> findByNameContainingIgnoreCase(String part);   // WHERE LOWER(name) LIKE %?%\n  long countByActiveFalse();                                // SELECT COUNT(*) WHERE active = false\n  boolean existsByEmail(String email);                      // efficient EXISTS check\n\n  @Query(\"select u from User u where u.email like %:domain\")  // JPQL escape hatch\n  List<User> inDomain(@Param(\"domain\") String domain);\n}" },
    { type: "callout", variant: "rule", title: "Name it, until you can't read it", text: "Derived names are perfect up to two or three conditions. The moment a method reads like <code>findByActiveTrueAndRoleInAndCreatedAfterOrderByNameDesc</code>, switch to <code>@Query</code> — same startup validation, human-readable intent. The grammar is a convenience, not a dare." },
    { type: "h3", text: "Pagination & sorting — never return a million rows" },
    { type: "p", text: "Any list endpoint that returns <em>everything</em> is a future outage. Spring Data bakes paging into the type system: accept a <code>Pageable</code>, return a <code>Page&lt;T&gt;</code>, and the LIMIT/OFFSET SQL, the count query, and the metadata (total pages, current index) all happen for you — Boot even builds the <code>Pageable</code> straight from <code>?page=2&amp;size=20&amp;sort=name,asc</code> in a controller:" },
    { type: "code", lang: "java", text: "Page<User> page = repo.findByActiveTrue(\n    PageRequest.of(2, 20, Sort.by(\"name\").ascending()));   // page index 2, 20 per page\n\npage.getContent();        // the 20 users\npage.getTotalElements();  // e.g. 8_431\npage.getTotalPages();     // 422\npage.hasNext();           // true" },
    { type: "h3", text: "Projections — fetch only what you need" },
    { type: "p", text: "Loading whole entities to show two columns wastes memory and I/O. An <strong>interface projection</strong> declares just the getters you want; Spring Data narrows the generated SQL to those columns:" },
    { type: "code", lang: "java", text: "interface UserSummary {          // just a shape — no implementation needed\n  Long getId();\n  String getName();\n}\n\ninterface UserRepository extends JpaRepository<User, Long> {\n  List<UserSummary> findByActiveTrue();   // SELECT id, name FROM users WHERE active = true\n}" },
    { type: "h3", text: "Hibernate essentials — what's under the repository" },
    { type: "p", text: "Every entity object is in one of three states, and half of all JPA confusion is state confusion. <strong>Transient</strong>: just <code>new</code>-ed, unknown to JPA. <strong>Managed</strong>: loaded or saved inside a transaction — Hibernate watches it, and any setter you call is automatically written to the database when the transaction commits (<em>dirty checking</em> — no explicit <code>save</code> needed). <strong>Detached</strong>: the transaction ended; the object still exists in Java but changes are no longer tracked." },
    { type: "cells", title: "Entity states", cells: [
      { v: "transient", dim: true }, { v: "managed", hl: 1 }, { v: "detached", hl: 2 }
    ], pointers: [{ i: 1, t: "dirty-checked" }], caption: "save()/find() moves an object into managed; commit or session close moves it out. Only managed objects auto-persist changes." },
    { type: "p", text: "Three more pieces of the machinery you will meet immediately. <strong>Lazy vs eager loading</strong>: relationship fields (a user's orders) are by default <em>lazy</em> — fetched only when touched — because eagerly loading object graphs would drag half the database into memory; touch a lazy field after the transaction closed and you get the famous <code>LazyInitializationException</code> (the full story lives in the Hibernate guide). <strong>Cascading</strong>: <code>cascade = CascadeType.ALL</code> on a parent-child relation makes persist/delete flow from parent to children, and <code>orphanRemoval = true</code> deletes a child the moment you remove it from the parent's collection — powerful, and meant for true ownership only. <strong>Connection pooling</strong>: opening a database connection costs milliseconds you can't spend per request, so Boot ships <strong>HikariCP</strong> — a pool of ~10 pre-opened connections that requests borrow and return — configured and running before you think about it." },
    { type: "h3", text: "Databases — H2 to develop, PostgreSQL/MySQL to run" },
    { type: "p", text: "<strong>H2</strong> is a database that lives inside your JVM — zero install, starts instantly, vanishes on shutdown. It makes the first hour of development and every repository test frictionless. Real deployments point the same code at PostgreSQL or MySQL purely through configuration — driver dependency plus datasource properties:" },
    { type: "code", lang: "yaml", text: "# dev (default profile) — in-memory H2, schema generated from entities\nspring:\n  datasource:\n    url: jdbc:h2:mem:dev\n  jpa:\n    hibernate:\n      ddl-auto: update        # convenient for dev ONLY\n\n---\n# application-prod.yml — real PostgreSQL\nspring:\n  datasource:\n    url: jdbc:postgresql://db.internal:5432/shop\n    username: ${DB_USER}\n    password: ${DB_PASSWORD}\n  jpa:\n    hibernate:\n      ddl-auto: validate      # NEVER let Hibernate alter prod schemas" },
    { type: "callout", variant: "pitfall", title: "ddl-auto is not a migration tool", text: "<code>ddl-auto: update</code> lets Hibernate mutate your schema to match your entities — fine for a laptop, catastrophic for production (it can't rename, won't shrink, and gives you no history or rollback). Production schemas change through migration tools (Flyway/Liquibase) with versioned SQL scripts, while Hibernate runs in <code>validate</code> mode: check the mapping, touch nothing." },
    { type: "h3", text: "Transactions — all or nothing" },
    { type: "p", text: "Transfer money between two accounts and crash between the debit and the credit — without a transaction, money evaporates. A transaction brackets several statements into one atomic unit, and the guarantees go by <strong>ACID</strong>:" },
    { type: "table", headers: ["Property", "Plain English"], rows: [
      ["<strong>A</strong>tomicity", "All statements commit, or none do — no half-transfers"],
      ["<strong>C</strong>onsistency", "Every commit moves the DB from one valid state to another (constraints hold)"],
      ["<strong>I</strong>solation", "Concurrent transactions don't see each other's unfinished work"],
      ["<strong>D</strong>urability", "Once committed, it survives a crash — it's on disk, not in hope"]
    ] },
    { type: "code", lang: "java", text: "@Service\nclass TransferService {\n  private final AccountRepository accounts;\n  TransferService(AccountRepository accounts) { this.accounts = accounts; }\n\n  @Transactional                          // one atomic unit — both rows or neither\n  void transfer(long fromId, long toId, BigDecimal amount) {\n    Account from = accounts.findById(fromId).orElseThrow();\n    Account to   = accounts.findById(toId).orElseThrow();\n    from.withdraw(amount);                // managed entities: dirty checking\n    to.deposit(amount);                   // writes both on commit — no save() calls\n    // any RuntimeException here rolls BOTH changes back\n  }\n}" },
    { type: "p", text: "<code>@Transactional</code> belongs on <strong>service methods</strong> — the unit of business work — not on controllers (too coarse, holds connections through serialization) or repositories (too fine, each call already gets one). By default it rolls back on unchecked exceptions only; add <code>rollbackFor</code> if you throw checked ones, and mark pure reads <code>readOnly = true</code> so Hibernate can skip dirty-checking. One trap to know before it bites: Spring applies transactions through a <strong>proxy</strong> around the bean, so a method calling another <code>@Transactional</code> method <em>on the same class</em> bypasses the proxy — the annotation silently does nothing. Route the call through another bean, or restructure." },
    { type: "table", headers: ["Isolation level", "Prevents", "Notes"], rows: [
      ["<code>READ_UNCOMMITTED</code>", "nothing — can read uncommitted \"dirty\" data", "Almost never used"],
      ["<code>READ_COMMITTED</code>", "dirty reads", "Default in PostgreSQL/Oracle — the usual choice"],
      ["<code>REPEATABLE_READ</code>", "+ non-repeatable reads (row changing between two reads)", "Default in MySQL/InnoDB"],
      ["<code>SERIALIZABLE</code>", "+ phantoms — as if transactions ran one at a time", "Safest, slowest; expect retries under contention"]
    ] },
    { type: "h3", text: "And JDBC? (the roadmap says: not needed)" },
    { type: "p", text: "Before ORMs, Java talked to databases through raw JDBC, and Spring's <code>JdbcTemplate</code> made that bearable: you write the SQL string, a <code>RowMapper</code> converts each <code>ResultSet</code> row into an object, and the template handles connections and exceptions. For CRUD applications Spring Data JPA has simply replaced this — that's why the roadmap crosses it out. But \"not needed to learn first\" isn't \"useless\": for bulk operations, reporting queries, or database features an ORM maps poorly, dropping to <code>JdbcTemplate</code> (or its modern cousin <code>JdbcClient</code>) is the honest, fast tool. Know it exists; reach for it deliberately." },
    { type: "callout", variant: "pro", title: "Log the SQL, at least once", text: "Set <code>spring.jpa.show-sql: true</code> (dev only) and read what Hibernate actually emits for your innocent-looking code. This one habit teaches you dirty checking (updates you never wrote), lazy loading (selects appearing mid-loop), and is how you'll catch the N+1 problem — the performance bug so classic it gets its own treatment in the Hibernate guide." },
    { type: "check", items: [
      { q: "JPA vs Hibernate vs Spring Data JPA — untangle the three.", a: "JPA is the standard API (annotations, EntityManager). Hibernate implements it and generates SQL. Spring Data JPA sits on top, generating repository implementations from your interfaces." },
      { q: "Why does modifying a loaded entity inside @Transactional persist without save()?", a: "Inside the transaction the entity is <strong>managed</strong> — Hibernate dirty-checks it and flushes any changes as UPDATEs at commit." },
      { q: "How does findByEmailAndActiveTrue work with no implementation anywhere?", a: "Spring Data parses the method name against the entity's fields at startup and generates the query — invalid names fail the boot, not production." },
      { q: "Why is ddl-auto: update banned in production?", a: "It mutates live schemas with no versioning, no rollback, and no rename support. Production uses migration scripts (Flyway/Liquibase) while Hibernate only validates." },
      { q: "A @Transactional method calls another @Transactional method in the same class — what happens?", a: "The inner annotation is ignored: transactions are applied by a proxy, and self-invocation never crosses the proxy. Move the inner method to another bean." }
    ] },
    { type: "ladder", steps: [
      { t: "Learn", d: "Say the three entity states and what dirty checking does, then explain ACID with the money-transfer story — no notes." },
      { t: "Drill", d: "Build User + UserRepository on H2, write three derived queries and one @Query, page the results, and read the generated SQL with show-sql on." },
      { t: "Interview-ready", d: "Own @Transactional: where it goes, rollback rules, readOnly, and the self-invocation trap. Then go meet the engine underneath.", href: "#/frameworks/spring-boot/hibernate", link: "Hibernate Under the Hood" },
      { t: "Master", d: "Take one endpoint from entity-returning to projection-returning and measure the SQL difference — then explain when the optimization is worth it.", href: "#/database", link: "Databases & SQL" }
    ] }
  ]
});
